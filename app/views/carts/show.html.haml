- if notice
  %p#notice= notice
%h2 Your Pragmatic Cart

%ul 
  - @cart.line_items.each do |item|
    %li #{item.quantity} &times #{item.product.title}


/ = link_to 'Edit', edit_cart_path(@cart)
/ |
/ \#{link_to 'Back', carts_path}
